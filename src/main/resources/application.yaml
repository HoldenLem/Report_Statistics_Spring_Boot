spring:
  data:
    mongodb:
      uri: mongodb://user123:password123@localhost:27017/statistics
      database: statistics
    redis:
      repositories:
        enabled: false
      time-to-live: 60000
      host: localhost
      port: 6379
  cache:
    type: redis
logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.springframework.data.redis: DEBUG
    org.springframework.data.mongodb: DEBUG
    org.mongodb.driver: DEBUG